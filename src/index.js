import React from 'react';
import ReactDOM from 'react-dom/client';
import AppInitializers from './js/AppInitializers';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AppInitializers />
  </React.StrictMode>
);
